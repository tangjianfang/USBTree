name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 全库一致性校验（链接/围栏/图谱/frontmatter/标题）
        run: bash tools/validate.sh
